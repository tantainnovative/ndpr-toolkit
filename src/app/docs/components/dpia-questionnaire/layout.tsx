import metadata from './metadata';

export { metadata };

export default function DPIAQuestionnaireLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      {children}
    </>
  );
}
